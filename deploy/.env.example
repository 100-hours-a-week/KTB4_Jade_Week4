# 배포 태그만 둔다. 비밀값은 app.env / mysql.env 에 있다.
# compose 의 ${...} 치환은 이 파일만 읽기 때문에 태그는 여기 있어야 한다.
# 프론트와 백엔드 워크플로는 각각 inactive 색상의 태그만 수정한다.

BACKEND_BLUE_TAG=sha-initial
BACKEND_GREEN_TAG=sha-initial
FRONTEND_BLUE_TAG=sha-initial
FRONTEND_GREEN_TAG=sha-initial
