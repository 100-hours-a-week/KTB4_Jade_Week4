# MySQL 컨테이너 초기화용. chmod 600. git 에 올리지 않는다.
# 이 값들은 볼륨이 처음 생성될 때만 반영된다. 나중에 바꿔도 기존 계정은 그대로다.
# MYSQL_PASSWORD 는 app.env 의 DATABASE_PASSWORD 와 같아야 한다.

MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=banteum
MYSQL_USER=banteum
MYSQL_PASSWORD=
