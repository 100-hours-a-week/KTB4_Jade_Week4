# 추후 수정사항

## 투표 진행 중 선택지 수정

- 현상: `PATCH /articles/{uuid}` 로 `optionA`, `optionB` 를 바꿔도 `ArticleVoteCount` 의 `countA`, `countB` 는 그대로 남는다.
- 문제: "치킨 3표" 였던 게시글이 "짜장면 3표" 가 된다. 이미 투표한 사람의 의사와 다른 결과가 노출됨.
- 수정 방향(택 1): 투표가 1건이라도 있으면 선택지 수정 차단, 또는 선택지 변경 시 투표 이력·집계 초기화.
